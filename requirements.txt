# --- Core API & Config ---
fastapi==0.110.0
uvicorn[standard]==0.29.0
pydantic==2.6.4
pydantic-settings==2.2.1
PyYAML==6.0.1

# --- Data Ingestion (PDF Parsing & Chunking) ---
pymupdf4llm==0.0.17
langchain-text-splitters==0.0.1

# --- Vector Database ---
qdrant-client==1.8.2

# --- LLM & Embedding Client ---
# Ghi chú: Ollama, vLLM và Infinity Server đều hỗ trợ API chuẩn của OpenAI
# Nên chỉ cần dùng chung 1 client là gọi được tất cả
openai==1.14.2

# --- Evaluation ---
ragas==0.1.5
datasets==2.18.0

# --- Observability / Logging ---
python-json-logger==2.0.7