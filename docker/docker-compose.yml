version: '3.8'

services:
  qdrant:
    image: qdrant/qdrant:v1.12.0
    container_name: qdrant_db
    ports:
      - "6333:6333" #Rest API
      - "6334:6334" #gRPC
    volumes:
      - ../qdrant_storage:/qdrant/storage
    restart: always
